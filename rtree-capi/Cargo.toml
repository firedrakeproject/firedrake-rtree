[package]
name = "rtree-capi"
version = "0.1.0"
edition = "2021"

[dependencies]
rstar = "0.12.2"
interval-tree = { path = "../interval-tree" }

[build-dependencies]
cbindgen = "0.29"

[lib]
name = "rtree_capi"
crate-type = ["cdylib"]
